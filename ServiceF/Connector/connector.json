{
  "name": "JdbcSinkConnectorService",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "value.converter": "io.confluent.connect.avro.AvroConverter",
    "value.converter.schema.registry.url": "${SCHEMA_REGISTRY}",
    "value.converter.enhanced.avro.schema.support": "true",
    "value.converter.subject.name.strategy": "io.confluent.kafka.serializers.subject.TopicNameStrategy",
    "errors.tolerance": "none",
    "errors.log.enable": true,
    "errors.log.include.messages": true,
    "topics": "kafkatest",
    "connection.url": "${MYSQL_HOST}",
    "connection.user": "${MYSQL_USER}",
    "connection.password": "${MYSQL_PASSWORD}",
    "table.name.format": "kafkatest",
    "auto.create": "true",
    "auto.evolve": "true"
  }
}